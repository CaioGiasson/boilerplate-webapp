/** Presigned GET TTL for private owner files. */
export const STORAGE_PRESIGN_TTL_PRIVATE_SECONDS = 15 * 60

/** Presigned GET TTL for avatars shown in profile / sidebar. */
export const STORAGE_PRESIGN_TTL_AVATAR_SECONDS = 60 * 60

/** Safety skew subtracted from in-process presign cache TTL (DATA-07). */
export const STORAGE_PRESIGN_CACHE_SKEW_SECONDS = 30
